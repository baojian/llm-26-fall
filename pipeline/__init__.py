"""Course data pipeline: the components the class reuses from Week 2 to the 1B run.

Standard library only, so every module runs on a student laptop and in the
lecture notebooks. Heavier tokenizers plug in through a callable that maps a
string to a list of integer ids (see ``pipeline.eval.make_tokenizer``).
"""
